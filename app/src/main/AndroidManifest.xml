<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.StartActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.linear.LinearSingleActivity" />
        <activity android:name=".ui.linear.LinearMultiActivity" />
        <activity android:name=".ui.grid.GridMultiActivity" />
        <activity android:name=".ui.grid.GridSingleActivity" />
        <activity android:name=".ui.EmptyDemoActivity" />
        <activity android:name=".ui.staggered.StaggeredSingleActivity" />
        <activity android:name=".ui.staggered.StaggeredMultiActivity" />
    </application>

</manifest>